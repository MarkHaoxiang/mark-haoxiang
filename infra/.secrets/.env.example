# Copy to .env (kept here, git-ignored) and fill in. Mounted read-only into the
# settlrl container at /app/.secrets/.env, where the app reads it. Real
# environment variables (the compose `environment:` block) override these.

# Comma-separated emails granted admin (superuser) on register / login.
SETTLRL_APP_ADMIN_EMAILS=you@example.com
